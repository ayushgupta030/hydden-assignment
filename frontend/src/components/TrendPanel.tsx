/**
 * Shows how the population is distributed, so that trends are obvious at a
 * glance rather than something the reader has to reconstruct from rows.
 *
 * Pick whatever form makes the shape of the data readable. The backend can
 * aggregate for you over the whole population -- see store.GroupCount -- so
 * this does not have to be computed from the page you happen to be showing.
 */
export default function TrendPanel() {
  // render the distributions here
  return null;
}
